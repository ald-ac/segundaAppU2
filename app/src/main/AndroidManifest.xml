<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appsmoviles.segundaappu2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SegundaAppU2">
        <activity
            android:name=".CanvasActivity"
            android:label="Dibujo en canvas"
            android:exported="true" />
        <activity
            android:name=".AnimacionActivity"
            android:exported="true"
            android:label="Animacion" />
        <activity
            android:name=".TouchSonidoActivity"
            android:exported="true"
            android:label="Touch y Sonido" />
        <activity
            android:name=".VideoActivity"
            android:exported="true"
            android:label="Reproductor de Video" />
        <activity
            android:name=".MusicaActivity"
            android:exported="true"
            android:label="Reproductor Musical" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>